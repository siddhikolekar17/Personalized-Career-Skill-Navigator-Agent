# Contributing Guidelines

## Setup

1. Clone the repository.
2. Create a Python virtual environment.
3. Install the required dependencies.
4. Run the Streamlit application locally.

## Branching

Create a separate branch for each feature, improvement, or documentation change.

Use a clear branch name that describes the contribution.

## Testing

Run the project tests before creating a pull request:

```bash
pytest